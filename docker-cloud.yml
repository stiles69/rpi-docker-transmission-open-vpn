version: '2'

services:
  transmission:
    image: stiles/rpi-docker-transmission-openvpn
    cap_add:
      - NET_ADMIN
    devices:
      - '/dev/net/tun'
    restart: always
    ports:
      - '9091:9091'
      - '8888:8888'
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - /torrents/:/data
    target_num_containers: 4
    environment:
      - OPENVPN_PROVIDER=PIA
      - OPENVPN_CONFIG=CA\ Toronto
      - OPENVPN_USERNAME=p2685989
      - OPENVPN_PASSWORD=Samsung#2013    
      - OPENVPN_OPTS=--inactive 3600 --ping 10 --ping-exit 60
      - LOCAL_NETWORK=10.0.0.0/24
  proxy:
    image: stiles/rpi-docker-transmission-openvpn-proxy
    links:
      - transmission
    ports:
      - '8080:8080'
    volumes:
      - /ettc/localtime:/etc/localtime:ro
    roles:
      - global

